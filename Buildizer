build_type: fpm
package_name: runtex
package_cloud: flant/main

fpm_conflicts: [flant-scripts-runtex flant-scripts-runtex2]

fpm_files:
  /usr/bin/runtex: /package/runtex
  /usr/bin/runtex2: /package/runtex

os:
  ubuntu:
    prepare:
      - apt-get install -y psmisc
  centos:
    prepare:
      - yum install -y psmisc

target:
- ubuntu-12.04
- ubuntu-14.04
- centos-centos6
- centos-centos7
